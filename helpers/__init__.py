# note: i prefer to import consts separately, so the 5th line is not really needed,
# but i decided to keep it in case i will forget or something

from .window import *
from .consts import *
from .window_tasks import *